#include <stdio.h>

/**
 * main - number combinations
 * return: return 0
 */
int main(void)
{
	int f;

	for (n = 28; n < 48; n++)
	{
		putchar(f);
		if (n != 47)
		{
			putchar(',');
			putchar(' ');
		}
	putchar('\n');

	return (0);
}
